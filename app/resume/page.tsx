// Dedicated resume route preserves the existing portfolio-to-resume navigation path.
import ResumeContent from '@/components/ResumeContent';

export default function ResumePage() {
  return <ResumeContent />;
}
