// Route-level loading screen keeps the existing terminal boot aesthetic during page transitions.
import Loading from '@/components/Loading';

export default function LoadingPage() {
  return <Loading />;
}
